from setuptools import setup

setup(
    name='tomomisaweddingapp',  # Name can be anything
    packages=['tomomisaweddingapp'],
    include_package_data=True,
    install_requires=[
        'flask',
    ],
)
